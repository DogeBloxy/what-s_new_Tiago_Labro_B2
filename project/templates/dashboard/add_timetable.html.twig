{% extends 'base.html.twig' %}

{% block title %}Ajouter un emploi du temps{% endblock %}

{% block body %}
<div class="container my-4">

<h1>Ajouter un emploi du temps</h1>

{{ form_start(TimetableForm) }}
    {{ form_row(TimetableForm.title) }}
    {{ form_row(TimetableForm.thumbnail) }}
    {{ form_row(TimetableForm.submit) }}

{{ form_end(TimetableForm) }}

</div>
{% endblock %}